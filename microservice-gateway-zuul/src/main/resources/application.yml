server:
  port: 8040
spring:
  application:
    name: microservice-gateway-zuul
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
management:
  security:
    enabled: false #以免查看zuul映射的路由列表时出401错误